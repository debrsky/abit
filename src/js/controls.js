import Controls from './svelte/controls.svelte';

// eslint-disable-next-line no-unused-vars
const controls = new Controls({
  target: document.getElementById('controls')
});

console.log('controls');
